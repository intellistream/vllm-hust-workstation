# ======================================================
#  sagellm-workstation  —  私有 AI 工作站配置
# ======================================================
#  复制本文件为 .env 后修改，然后 docker compose up -d

# sagellm-gateway 的地址（OpenAI 兼容接口）
# 如果 gateway 跑在宿主机，Docker 内用 host.docker.internal
SAGELLM_BASE_URL=http://host.docker.internal:8080

# API Key（sagellm 在私有部署时通常不需要）
SAGELLM_API_KEY=not-required

# 默认加载的模型 ID（与 gateway 中注册的模型名一致）
DEFAULT_MODEL=Qwen2.5-72B-Instruct

# 后端类型标签（显示在监控面板，仅展示用）
BACKEND_TYPE=Ascend 910B

# 前端监听端口
APP_PORT=3000

# ── 白牌定制 ──────────────────────────────────────────
# 左上角品牌名
APP_BRAND_NAME=SageLLM 工作站

# Logo URL（留空则显示默认 AI 图标）
# 支持 http/https URL 或 base64 data URI
APP_BRAND_LOGO=

# 主题色（Hex）
APP_ACCENT_COLOR=#6366f1
